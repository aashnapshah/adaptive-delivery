# The pipeline stages run fully offline with the standard library (stub backend). The only
# hard requirements are numpy + matplotlib (bandit + figures) and flask (the app).

numpy>=1.21          # contextual bandit (policy stage)
matplotlib>=3.5      # evaluation plots + exported result figures
python-dotenv>=1.0   # loads the repo-root .env (model keys)
flask>=2.0           # the localhost study/demo app (app/)

# Optional
ollama>=0.3          # only if you use a local model via Ollama (required for MIMIC runs)
waitress             # production WSGI server for the app
pandas>=1.3          # only to BUILD MIMIC cases from raw CSVs (harness/cases/mimic.py --build)
